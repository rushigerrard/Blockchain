#Boost installation:
sudo apt-get install libboost-all-dev

#Addding firewall setting in iptables to allow to start pisthach server:
#sudo iptables -A vcl-inuse -p tcp -s <ip of node which you want to allow> -j ACCEPT
#iptables save
